import { Route } from '@angular/router';

import { UserManagementComponent } from './index';

export const UserManagementRoutes: Route[] = [
	{
		path: 'usermanagement',
		component: UserManagementComponent
	}
];
